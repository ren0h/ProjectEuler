Multiples of 3 or 5 (https://projecteuler.net/problem=1)

var naturalNumbersRange = 1..<1000
var sum = 0
for number in naturalNumbersRange {
    let mod5 = number % 5
    let mod3 = number % 3
    if mod3 == 0 || mod5 == 0{
        sum += number
    }
}
print(sum)
